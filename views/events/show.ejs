<%- include('../partials/header') %>

<div class="container text-center">
    <div class="row">
        <div class="col-md">
            <h1>Selected Event Details</h1> <div class="d-grid gap-2 col overflow-auto">
                <a class="btn btn-primary" href="/events/<%= event._id %>/edit">Edit event details</a>
                <%- include('../events/partials/selected') %>
            </div>
        </div>
        <div class="col-md">
            <h1>Current events</h1>
            <div class="d-grid gap-2 col overflow-auto">
                <a class="btn btn-primary" id="add-event" href="/events/new">Add event</a>
                <%- include('../events/partials/allevents') %>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>